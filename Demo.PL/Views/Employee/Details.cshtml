@model EmployeeViewModel

@{
    ViewData["Title"] = "Details";
    var departments = new SelectList(ViewBag.Departments, "Id", "Name");
}

<h1 class="text-center">Employee @Model.Name Details</h1>
<br />

<div class="row">
    <div class="col-12">

        <partial name="PartialViews/DeleteDetailsPartialView" />

        <br />
        <a class="btn btn-success" asp-action="Update" asp-route-id="@Model.Id">Update</a>
        <a class="btn btn-light" asp-action="Index">Back To List</a>

    </div>
</div>


